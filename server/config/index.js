const resCodes = require('./responseCodes');

const mysqlConfig = {
  host: 'localhost',
  user: 'chenjg',
  password: 'chenjg',
  database: 'sparker',
};

module.exports = {
  mysqlConfig,
  resCodes,
};
